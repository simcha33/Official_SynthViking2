using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RewardPilar : MonoBehaviour
{

/*
    EnemyManager enemyBehaviourManagerScript;
    EnemySpawnManager enemySpawnManagerScript;
    RewardPilarManager rewardPilarManager; 
    public Transform spawnPoint;
    public RewardPilar thisScript;
    // Vector3 spawnDirection; 

    void Awake()
    {
        rewardPilarManager = GameObject.Find("RewardPilarManager").GetComponent<RewardPilarManager>();
        //enemyBehaviourManagerScript = GameObject.Find("EnemyBehaviourManager").GetComponent<EnemyManager>();
    }

    private void Start()
    {
        thisScript = GetComponent<RewardPilar>();
        rewardPilarManager.activePilar = thisScript; 
    }

    // Update is called once per frame
    void Update()
    {

    }
    */
}
